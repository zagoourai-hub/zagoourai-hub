# Checks

- Both supplied assets are referenced with relative paths and descriptive alt text.
- No fake stars, followers, commits, contribution counts, or availability claims appear.
- The top bar uses only real profile and repository destinations, never simulated Star, Fork, or Watch controls.
- No emoji is used as an interface icon.
- No custom CSS, JavaScript, scripts, or unsupported layout behavior is required.
- The visual accent comes from the supplied images, not a competing badge palette.
- The first screen states identity, role, location, and a real GitHub destination.
- The hero uses the supplied monogram and a short truthful terminal profile snippet.
- Tables contain only short paired content so GitHub's mobile table behavior remains safe.
- All selected-work links point to public repositories confirmed on the profile.
